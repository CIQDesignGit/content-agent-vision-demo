/** Primary “Review SKUs” CTA — matches stream detail `Button` styling. */
export const reviewSkuCtaClassName =
  "group inline-flex h-9 shrink-0 items-center justify-center gap-1.5 rounded-lg bg-brand-800 px-3.5 text-sm font-semibold text-white transition-colors hover:bg-brand-900 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand-800"
